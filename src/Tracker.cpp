#include "../include/Tracker.hpp"

void Tracker::track(std::vector<LidarScan> const& lidarScans)
{
    // Grid grid;
    
    // for (auto scan : lidarScans)
    // {
    //     grid.populateGrid(scan);
    // }
    
}